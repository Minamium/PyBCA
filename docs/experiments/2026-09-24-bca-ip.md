# BCA-IP・512試行×300万ステップの実験

## 現在の状態

- 対象: 現在の `Sample/Cellspace/BCA-IP.yaml`、base-rule、既存 44 イベント。
- 本番の指定: 3,000,000 ステップ、V100 8 台、24 時間以内で可能な最大試行数。
- 検証済み計算コアの基点: `dba815081223c60beb49529ca3cc49ab45e55394`。今回 core / api は変更していません。
- rokko の専用配置: `/home/IM25D029/PyBCA_workspcace/bca-ip-statistics-20260924/`。
- **容量・時間測定 `236781.rokko1` は正常終了**。2026-09-24 14:49:49 JST 開始、10分11秒、終了コード0。[計測結果](2026-09-24-capacity/plan.json)。
- **確率0.5の8 GPU単一ジョブ `236962.rokko1` を2026-09-25 00:29:35 JST投入済み**。512 trials、各3,000,000ステップ、上限24時間。00:48:59時点では `Q`、8 GPU要求に対し空き4台の資源待ちです。[再投入記録](2026-09-25-production-p05.json)。
- 旧 `236862.rokko1` は確率1.0で約114〜115万更新まで実行し、出力0件を確認したため停止。全8 rankの最終checkpointと履歴を保持しています。[9/24の投入記録](2026-09-24-production.json)は投入直後の記録で、現在の状態を示すものではありません。
- 同じ2試行・10万更新で確率0.5と1.0を比較し、0.5でFSM→Amp出力27件を確認しました。[診断結果・論文との照合](2026-09-25-probability-check/report.md)。現行入力のbとWeightsはSI Instance 2に一致しますが、閾値・Amp制御と最適解判定の認定は未完了です。容量測定・診断・取り消したジョブは新しい512試行の標本には追加しません。
- 続く[イベント・位相診断](2026-09-25-event-audit/report.md)では、1.0でToken binと信号が逆位相となり観測用CJoinが本線を止める現象を、実回路とCPU referenceでも再現しました。44イベントの条件判定・記録・書込みは2実装の計88ケースで一致しています。

## 8 GPU の投入方法

`scripts/rokko/bca-ip-512.pbs` は、ユーザー指定の `/home/IM25D029/ws/bacteria_motion/chemotaxis_cuda_mpi/run_8gpu.pbs` 等と同じ資源指定です。

```text
select=1:ngpus=8:ncpus=8:mpiprocs=8:mem=64gb
place=free
walltime=24:00:00
```

G キューの `resources_max.nodect=1` に対して、先に試した `select=2` が拒否されました。これは「1ジョブではGPUを8台使えない」という意味ではありません。`select=1` で8台の要求は受理されました。途中で投入した4 GPUジョブ `236860` / `236861` は取り消し、単一ジョブ `236862` に置き換えています。分割ジョブの出力は別ディレクトリに残り、本番には混ぜません。

現在の本番スクリプトは `scripts/rokko/bca-ip-512-p05.pbs` です。`python -m torch.distributed.run --standalone --nproc-per-node=8` で起動し、総試行数512を各rank64に分け、seed `20260924`、試行ID `0..511`、`global_prob=0.5`、`cuda` / `independent`、candidate capacity 4096を使用します。1,000ステップごとにイベント履歴を保存し、10,000ステップごとに最新の再開用チェックポイントを更新します。

現在の出力先: `/home/IM25D029/PyBCA_workspcace/bca-ip-statistics-20260924/results/bca-ip-512-trials-p05-20260925/`。ログ: `results/logs/bca-ip-512-p05-236962.rokko1.log`。旧1.0の結果は `results/bca-ip-512-trials-20260924/` に保持しています。

## 試行数の決め方

確率1.0の単GPU実測では、64 trials/GPU は最も遅い反復で25.228 ms/step、ピークメモリ約1.33 GiBでした。300万ステップへの単純換算は約21.0時間です。保存・初期化と10%の速度変動余裕を加えた予測は23.14時間、さらに1時間を予約する予備測定の基準では24.14時間です。その基準での推奨は63 trials/GPU（合計504）でした。確率0.5の64 trials/GPUの速度は未確認であり、起動後に実測を更新する必要があります。

ユーザーの512 trialsで投入する指示に従い、64 trials/GPUを採用しています。24時間での完走を保証する測定ではなく、8GPU同時実行や長時間での速度変化によって未完了となる可能性があります。24時間で停止した未完了試行は最適解未到達の「失敗」には数えません。

`scripts/plan_bca_ip_capacity.py` は試行数を倍増して上限を挟み、整数単位で二分探索します。推薦するのは実際に計測した試行数だけです。各候補では通常の BCA-IP、独立乱数、イベント履歴の逐次保存を使い、1,000 ステップ × 3 回を同期して測ります。

判定式は次です。

```text
予測秒数 = 3,000,000 × (最も遅い反復の秒/step + checkpoint秒/10,000) × 1.10
           + 初期化秒 + 最終checkpoint秒 + 3,600秒
予測秒数 <= 86,400秒
GPU peak allocated / reserved の大きい方 <= GPU総メモリの90%
```

10% の速度変動余裕と 1 時間の予約時間を確保します。履歴 flush は反復の計測時間に含まれます。独立した子プロセスで各候補を測り、前の候補の allocator を残しません。OOM は不適合として記録し、それ以外のエラーは測定失敗として停止します。

この単 GPU 測定は予備測定です。判定処理を含む8 GPU同時測定は未実施のため、予備測定JSONの `production_ready=false` はそのまま保存しています。本番の試行数は上記のユーザー指定で512に固定しました。

出力: `results/capacity-236781.rokko1/plan.json` と候補ごとの測定 JSON / ログ。

## 集計前に確定する判定

9/25にユーザー指定の論文原稿を確認しました。次の2条件があり、現在の入力のb=8とWeights=[4,4,5,1,1,1]はInstance 2に一致します。閾値・Amp制御を含む完全な認定は未完了です。

| 条件 | a | b | c |
| --- | --- | ---: | --- |
| Instance 1 | [1,1,2,2,4,4] | 6 | [2,3,4,5,1,1] |
| Instance 2 | [1,1,2,2,4,4] | 8 | [2,2,1,5,5,5] |

必要なのは現行セル空間の a, b, c、および x1…x6 を読むセル座標と状態、または既存の判定方法です。FSM の Key Token を直接読む場合は、その領域と有効な状態を指定します。出力イベントの時間窓から読む場合は、窓幅・閾値・reset 処理・観測時点を結果を見る前に固定し、その判定として到達時間を報告します。単なる出力イベント初出を最適解発見とは扱いません。

原稿の両問題について6 bit全64通りを列挙し、最適値はいずれも14、Instance 1の最適解は[1,1,1,1,0,0]、Instance 2は[1,1,0,1,0,1]または[1,1,0,1,1,0]と再確認しました。現行回路の認定後、A/Bのいずれかが条件を満たした最初の時刻を1 trialの到達時刻にし、A/Bを2個の独立試行として数えません。確率診断は本番と同じseed・IDを使っているため、追加の独立標本にはしません。

今回のrunnerにはKey Tokenの毎ステップ観測は実装されていません。チェックポイントは最新状態に更新する再開用で、過去の全状態を保存するものではありません。保存したイベントから定義できる到達時間は後で集計できますが、Key Tokenから厳密な初到達時刻を求めるには、判定定義を確定したうえで同じ試行ID・seedによる決定的な再実行が必要になる可能性があります。現時点では最適解到達を計測済みとは扱いません。

## 集計仕様

`scripts/summarize_bca_ip_trials.py` を追加しました。確定した decoder から、各独立試行につき次の JSONL を 1 行出す契約です。

```json
{"trial_id": 0, "observed_steps": 3000000, "first_hit_step": null, "decoder_sha256": "<判定定義のSHA-256>"}
```

到達時刻は完了した CA 更新回数で数え、初期状態が最適なら 0 とします。既存イベントログの 0 始まりの step は、そのまま到達更新回数にはしません。

- 成功: 制約を満たす最適解を 3,000,000 step 以内に観測。
- 失敗率: 全期間を観測して未到達だった試行の割合。Wilson 95% 区間を併記。
- 到達時間: 観測された成功試行の平均・中央値・標準偏差。未到達試行を含む無制限の平均とは区別。
- 3,000,000 step までの制限平均: 成功は到達時刻、未到達は 3,000,000 として平均する `mean(min(T_hit, horizon))`。
- 中断: 未到達のまま horizon より前に停止した試行は未完了。失敗に数えず、コホート全体の失敗率・制限平均は未確定として返す。
- 異なる decoder の混合、試行 ID 重複、予定試行数に足りない入力は拒否。

現在、この集計に渡せる認定済み到達時刻データはありません。人工データによる中断・未到達・境界時刻の 5 テストと、容量選択の 2 テストは合格しています。

```sh
PYTHONPATH=src python -m unittest discover -s tests -p 'test_capacity_planning.py' -v
PYTHONPATH=src python -m unittest discover -s tests -p 'test_bca_ip_statistics.py' -v
```
